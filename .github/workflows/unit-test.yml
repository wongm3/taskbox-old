name: Unit Tests

on:
  pull_request:
    branches: [master]

jobs:
  unit_test_pr:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 14
      - run: npm ci
      - run: npm t
